function calculateAverage(num1, num2) {
    return (num1 + num2) / 2;
}
let avg = calculateAverage(20, 50); // (20+50)/2 = 35
console.log(avg);
let avg2 = (num1, num2) => , { let, myavg = (num1 + num2) / 2 };
return myavg;
export {};
